# Personal credentials for the Knowledge Aggregator
# Copy this file to .env and fill in your personal values.
# DO NOT commit the actual .env file to version control.

# Trello credentials (https://trello.com/app-key)
TRELLO_API_KEY=YOUR_TRELLO_API_KEY
TRELLO_TOKEN=YOUR_TRELLO_TOKEN

# Google credentials for OAuth
# This should point to the client secrets file you downloaded from Google Cloud Console.
GOOGLE_CLIENT_SECRETS_JSON=auth/your_google_credentials.json

# --- Database Credentials ---
# Use a unique prefix for each database connection.
# The prefix (e.g., "SUPABASE") must match `connection_env_var_prefix` in your profile JSON.

# Default Supabase Connection
SUPABASE_DB_HOST=db.YOUR_PROJECT_REF.supabase.co
SUPABASE_DB_USER=postgres
SUPABASE_DB_PORT=5432
SUPABASE_DB_NAME=postgres
SUPABASE_DB_PASSWORD=YOUR_DATABASE_PASSWORD

# Example for another database project
# ANOTHERDB_DB_HOST=...
# ANOTHERDB_DB_USER=...
# ANOTHERDB_DB_PASSWORD=...
# ANOTHERDB_DB_PORT=...
# ANOTHERDB_DB_NAME=...

# Supabase credentials (shared part)
SUPABASE_ANON_KEY=YOUR_SUPABASE_ANON_KEY
SUPABASE_SERVICE_ROLE_KEY=YOUR_SUPABASE_SERVICE_ROLE_KEY

# Project-specific database passwords
# The key here (e.g., WA_ASSISTANT_DB_PASSWORD) must match `password_env_var` in your profile JSON
WA_ASSISTANT_DB_PASSWORD=YOUR_DATABASE_PASSWORD_HERE

# Repository paths
REPO_SITE=/path/to/your/site/repository
REPO_APP_SERVER=/path/to/your/app/server/repository

# For Supabase schema dump
# PG_PASSWORD="your_postgres_password"

# (Optional) GitHub token for fetching from private repos or to avoid rate limits.
# The script will prefer using the authenticated GitHub CLI (`gh`) if available.
GH_TOKEN="" 